<style>
h1 
{
    color: red;
}
h2 
{
    color: orange;
}
a 
{
    color: yellow !important;
}
em
{
    color: #7CFC00;
}
</style>




{{TOC}}

---

# **1. Опеределения** [⬆⬆⬆](#top)
## **1.1. Машина Тьюринга** [⬆⬆⬆](#top)

*Машина Тьюринга* есть кортеж вида:

$$
T = (Q, V, \star, \square, S, L, R, q_0, q_f, \delta),
$$

где:

$Q$ — конечное множество состояний;  
$V$ — конечный входной алфавит;  
$\star \notin V$ — символ маркера начала ленты;  
$\square \notin V$ — пустой символ (или пробел);  
$S, L, R \notin V$ — символы направления движения головки;  
$q_0 \in Q$ — начальное состояние;  
$q_f \in Q$ — заключительное состояние;  
$\delta$ — функция переходов, являющаяся отображением вида:
$$
\delta: Q \times (V \cup \{\star, \square\}) \to 2^{(Q \times (V \cup \{\star, \square\}) \times \{S, L, R\})},
$$

причем значение $\delta(q, a)$ — конечное (возможно, и пустое) множество упорядоченных троек из соответствующего декартова произведения.

Функция переходов может быть записана в виде системы команд.  
Каждая команда есть слово вида:

$$
qa \to rb, M,
$$

где:

$a, b \in V \cup \{\star, \square\}$,  
$M \in \{S, L, R\}$,  
$\to \notin V \cup \{\star, \square, S, L, R\}$.

Слово $qa$ (до стрелки) — левая часть команды,  
а слово $rb, M$ (после стрелки) — правая часть команды.

## **1.2. Конфигурация МТ и отношения выводимости на множестве конфигураций** [⬆⬆⬆](#top)

*Конфигурация машины Тьюринга $T$* — кортеж:

$$
(q, x, y) \in Q \times (V \cup \{\star, \square\})^* \times (V \cup \{\star, \square\})^*.
$$

Непосредственный вывод конфигурации:  
- $(q, x, ay) \vdash (r, x, by)$ при $qa \to rb, S \in \delta$;  
- $(q, xc, ay) \vdash (r, x, cby)$ при $qa \to rb, L \in \delta$;  
- $(q, x, acy) \vdash (r, xb, cy)$ при $qa \to rb, R \in \delta$.  

*Вывод на множестве конфигураций* — последовательность $C_0, C_1, ..., C_n, ...$, где $\forall i \geq 0$ выполняется $C_i \vdash C_{i+1}$, если $C_{i+1}$ существует.  

Конфигурация $C'$ выводится из $C$, если существует вывод $C = C_0 \vdash \dots \vdash C_n = C'$. Длина вывода — число $n$.  

Обозначения выводимости совпадают с обозначениями для конечных и магазинных автоматов.  

*Конфигурация* — тройка $(q, x, y)$, где $q$ — состояние, $x$ — часть цепочки на ленте до головки, $y$ — часть цепочки после головки, первый символ которой обозревается.  

По соглашению, цепочка $x\square^*$ (при $x \in (V \cup \{\star, \square\})^*$) отождествляется с $x$. Допустимо отбрасывание пустых символов в конце слова.

## **1.3. Вычислимость по Тюрингу** [⬆⬆⬆](#top)

*Словарная (вербальная) функция в алфавите $V$* — частичное отображение множества слов в алфавите $V$ в себя.

Словарная функция $\varphi$ в алфавите $V$ называется *вычислимой по Тьюрингу*, если существует машина Тьюринга $T_{\varphi}$ с входным алфавитом $V'$, содержащим $V$, такая, что:

$$
x \in D(\varphi) \iff \exists T_{\varphi}(x) \land (T_{\varphi}(x) = \varphi(x)).
$$

## **1.4. Нормальный алгорифм Маркова (НА)** [⬆⬆⬆](#top)

*Нормальный алгорифм (НА) в алфавите $V$* — упорядоченная тройка $A = (V, S, P)$, где:  
$S$ — схема НА, представляющая конечную последовательность формул подстановки в $V$ с выделенными заключительными формулами;  
$P$ — частичный кортеж (подпоследовательность) формул из $S$.  

Заключительная формула в схеме отмечается точкой после стрелки.  

*Общий вид схемы НА:*

$$
A: 
\begin{cases}
u_1 \to [\bullet] v_1 \\
u_2 \to [\bullet] v_1 \\
\vdots \\
u_n \to [\bullet] v_n
\end{cases}
$$

## **1.5. Процесс работы НА со словом** [⬆⬆⬆](#top)

*Процесс работы нормального алгорифма $A$ со словом $x$* — конечная или бесконечная последовательность слов $x = x_0, x_1, ..., x_n, ...$, где $n \geq 0$, такая, что для каждого $i \geq 0$ выполняется $A : x_i \vdash x_{i+1}$ или $A : x_i \vdash \bullet x_{i+1}$.  

Слово $x_i$ и все последующие считаются *неопределёнными*, если $A : x_{i-1} \vdash \bullet x_i$, то есть $x_i$ получено применением заключительной формулы, или $\sim A(x_i)$, то есть слово не поддаётся $A$ и процесс обрывается.  

Если процесс конечен, последнее слово обозначается $A(x)$ и называется *результатом работы $A$ со словом $x$*. В этом случае записывают $!A(x)$. Если процесс не завершается, записывают $\neg !A(x)$.

## **1.6. Вычислимость по Маркову** [⬆⬆⬆](#top)

Вербальная функция $f$ типа $(V, V)$ — *вычислимая по Маркову*, если существует нормальный алгорифм $A_f$ над алфавитом $V$, такой, что для любого слова $x \in V^*$ выполняется:

$$
!A_f(x) \iff x \in D(f),
$$

где в этом случае:

$$
A_f(x) = f(x).
$$
# **2. Теоретические вопросы** [⬆⬆⬆](#top)

## **2.1. Теорема композиции НА. Формулировка и доказательство** [⬆⬆⬆](#top)

*Теорема:* Каковы бы ни были нормальные алгорифмы $A$ и $B$ в алфавите $V$, может быть построен такой нормальный алгорифм $C$ над алфавитом $V$, что для любого слова $x \in V^*$ имеет место условное равенство:
$$
C(x) \simeq B(A(x))
$$

$\square$ Рассмотрим построение схемы алгорифма $C$ «на уровне идеи». 

Все
сочетаемые нормальные алгорифмы заданы в одном и том же алфавите $V$.

По построению она является схемой нормального алгорифма в алфавите: 
$$
V \cup \overline{V} \cup \{\alpha, \beta\},
$$
где $\overline{V}$ — алфавит, составленный из «букв-двойников» букв алфавита $V$, т.е. каждой букве $\xi \in V$ сопоставляется буква $\overline{\xi} \in \overline{V}$ - «двойник» буквы $\xi$ так, что соответствие взаимно однозначное и пересечение $V \cap \overline{V}$ пусто. 

Буквы $\alpha$ и $\beta$ считаются не принадлежащими алфавиту $V \cup \overline{V}$.

$A^\alpha$ — набор формул подстановок, образованный из схемы замыкания $A^\bullet$ алгорифма $A$ заменой всех «точек» буквой $\alpha$ => каждая заключительная формула вида $u \to \cdot v$ в схеме $A^\bullet$ заменяется простой формулой $u \to \alpha v$.

Простые формулы переносятся без изменений.

$\overline{B}_{\alpha}^{\beta}$ — набор формул подстановок, полученный из схемы замыкания $B^\bullet$ алгорифма $B$ путем замены в ней каждой буквы алфавита $V$ ее «двойником», всех «точек» буквами $\beta$ и после этого замены каждой простой формулы вида $\to v$ (пустая левая часть) формулой $\alpha \to \alpha \overline{v}$, а каждой заключительной формулы $\to \cdot \overline{v}$ с пустой левой частью формулой $\alpha \to \alpha \beta \overline{v}$.

Соответствующие таблицы:

| $A^\bullet$ | $A^\alpha$ |
|:---------:|:---------:|
| $u \to v$ | $u \to v$ |
| $u \to \cdot v$ | $u \to \alpha v$ |


| $B^\bullet$ | $\overline{B}_{\alpha}^{\beta}$ |
|:---------:|:----------------------------:|
| $u \to v \quad (u \neq \lambda)$ | $\overline{u} \to \overline{v}$ |
| $u \to \cdot v \quad (u \neq \lambda)$ | $\overline{u} \to \beta \overline{v}$ |
| $\to v$ | $\alpha \to \alpha \overline{v}$ |
| $\to \cdot \overline{v}$ | $\alpha \to \alpha \beta \overline{v}$ |

Любая вышележащая формула в схеме «перехватывает» управление у всех нижележащих.

Работа алгорифма $C$ с исходным словом:

$ \left\{ 
\begin{array}{ll} 
\xi \alpha \to \alpha \xi & (\xi \in V) \quad &(1) \\ 
\alpha \xi \to \alpha \overline{\xi} & (\xi \in V) \quad &(2) \\ 
\overline{\xi} \eta \to \overline{\xi} \overline{\eta} & (\xi, \eta \in V) \quad &(3) \\ 
\overline{\xi} \beta \to \beta \overline{\xi} & (\xi \in V) \quad &(4) \\ 
\beta \overline{\xi} \to \beta \xi & (\xi \in V) \quad &(5) \\ 
\xi \overline{\eta} \to \xi \eta & (\xi, \eta \in V) \quad &(6) \\ 
\alpha \beta \to \cdot & \quad &(7) \\ 
\overline{B}_{\alpha}^{\beta} & \quad &(8) \\ 
A^\alpha & \quad &(9) 
\end{array} 
\right. $

Пусть алгорифм $A$ применим к слову $x$. Так как алгорифм $A$ заменён его замыканием, а набор формул $A^\alpha$ построен по схеме замыкания алгорифма $A$, на последнем шаге работы $A^\bullet$ со словом $x$ применяется заключительная формула. 

Тогда на соответствующем шаге работы алгорифма $C$ со словом $x$ применяется формула вида $u \to \alpha v$, а если $A : x \models \cdot y$, то $C : x \models y_1 \alpha y_2$, где $y_1 y_2 = y$.

Применение формул (9) к слову $x$ приводит к эмуляции алгорифма $A$ алгорифмом $C$. 

В случае определения $y = A(x)$ на некотором шаге работы $C$ с $x$ получается слово $y_1 \alpha y_2$, где $y_1 y_2 = y$.

При появлении $\alpha$ в начале слова формулы (1) становятся неприменимыми, после чего работают формулы (2) и (3), преобразующие $y = A(x)$ в его двойник $\overline{y}$ в алфавите $\overline{V}$. 

Для $y = y(1)y(2)...y(m)$ выполняется $\overline{y} = \overline{y}(1)\overline{y}(2)...\overline{y}(m)$.

После этого применимы только формулы набора $\overline{B}_{\alpha}^{\beta}$, обеспечивающие эмуляцию работы алгорифма $B$ со словом $y = A(x)$. 

В частности, если $B : y \models w$, то $C : \alpha \overline{y} \models \alpha \overline{w}$.

Если $B : y \models \cdot z$, то $C : \alpha \overline{y} \models \alpha \overline{z_1} \beta \overline{z_2}$, где $z = z_1 z_2 = B^\bullet(y) = B^\bullet(A^\bullet(x))$.

Далее, посредством формул (5) и (6) буквы-двойники заменяются на буквы алфавита $V$, выводя из $\alpha \beta \overline{z}$ слово $\alpha \beta z$. 

Формулы (1)-(6) становятся неприменимыми, и применяется заключительная формула (7), приводящая к выводу:

$$
C : \alpha \overline{y} \models \alpha \overline{z_1} \beta \overline{z_2} \models \alpha \beta \overline{z} \models \alpha \beta z \vdash \cdot z = B(A(x)).
$$

Таким образом, если хотя бы одно из слов $A(x)$ или $B(A(x))$ не определено, т.е. $\neg!A(x)$ или $\neg!B(A(x))$, то $\neg!C(x)$, а если $!A(x)$ и $!B(A(x))$, то $C(x) = B(A(x))$, следовательно,

$$
C(x) \simeq B(A(x))
$$

доказано. $\blacksquare$


## **2.2. Эквивалентность НА. Замыкание НА, естественное и формальное распространение НА на более широкий алфавит. Доказать эквивалентность НА и его замыкания** [⬆⬆⬆](#top)

НА $A$ и $B$ типа $(V, V)$ над алфавитом $V$ называют *вполне эквивалентными относительно алфавита $V$*, если для всякого слова $x \in V^*$ имеет место $!A(x) \Leftrightarrow !B(x)$ и если выполняется $!A(x)$, то $A(x) = B(x)$.

---

*Замыканием нормального алгорифма* называется НА, схема которого получается из схемы исходного НА путем добавления после всех формул заключительной формулы $\to \bullet$. 

Замыкание НА $A$ обозначается $A^\bullet$. 

Легко показать, что эти НА вполне эквивалентны относительно алфавита, в котором задан исходный НА.

---

Для НА $A$ в алфавите $V$ можно той же самой схемой задать НА $A'$ в более широком алфавите (т.е. подавать на его вход слова в некотором расширении алфавита $V$). 

Тогда очевидно, что для любого слова $x$ в алфавите $V$ имеет место $A(x) \simeq A'(x)$. 

Таким образом, НА $A$ и $A'$ вполне эквивалентны относительно алфавита $V$. 

НА $A'$ называют при этом *естественным распространением* исходного на более широкий алфавит.

---

*Формальное распространение* $A^f$ НА $A$ на более широкий алфавит $V' \supset V$ определяется тем, что к схеме $A$, в начале её («сверху»), приписываются формулы $\eta \to \eta$ для всех букв $\eta$ из $V' \setminus V$.

 Тогда, как нетрудно видеть, $(\forall x \in V^*)(A^f(x) \simeq A(x))$, но, в отличие от естественного распространения, имеет место $(\forall y \in V' \setminus V)(\neg!A^f(x))$, то есть НА $A^f$ не применим ни к одному из слов, не являющимся словом в алфавите $V$.

---

Докажем, что *$A$ и его замыкание эквивалентны*: 

$\square$ Пусть $!A(x)$, значит $A: \sim A(x)$ (естественный обрыв) или $A: z \models \bullet A(x)$ для некоторого $z$ (на последнем шаге).

В ситуации естественного обрыва: $A^\bullet:  \models A(x) \vdash  \bullet A(x)$.  

В ситуации последнего шага: $A^\bullet : x \models z \vdash  \bullet A(x)$.  

Таким образом, если $!A(x)$, то $!A^\bullet(x)$ и $A(x) = A^\bullet(x)$. Если же $\neg!A(x)$, то очевидно что $\neg!A^\bullet(x)$, т.к. до команды $\to \bullet$ очередь не дойдёт. $\blacksquare$

## **2.3. Понятие перевода в двухбуквенный алфавит. Формулировка теоремы о переводе** [⬆⬆⬆](#top)

Пусть $V = \{a_1, \dots, a_n\}$ – исходный алфавит, а алфавит $V_{\alpha} = \{\alpha, \beta\}$ состоит из двух букв. Будем считать, что эти два алфавита не пересекаются .

Тогда назовем *переводом буквы $a_k \in V$ в алфавит $V_{\alpha}$* слово $[ a_k = \alpha \beta^k \alpha$. Перевод непустого слова $x = x(1)x(2)...x(m) \in V^*$ есть соединение переводов его букв:

$$
[ x = [ x(1) [ x(2) \dots [ x(m).
$$

---

*Теорема:* Каков бы ни был нормальный алгорифм $A$ типа $(V, V)$ над алфавитом $V$, может быть построен нормальный алгорифм $B$ в алфавите $V \cup V_{\alpha}$, вполне эквивалентный $A$ относительно алфавита $V$, то есть для любого слова $x \in V^*$ имеет место условное равенство $A(x) \simeq B(x)$.

## **2.4. Определения изображения и записи НА. Примеры. Формулировка теоремы об универсальном НА** [⬆⬆⬆](#top)

Пусть НА $A$ является алгорифмом в алфавите $V$.

*Изображение НА $A$* есть слово в алфавите $V \cup \{\alpha, \beta, \gamma\}$, где $\alpha, \beta, \gamma \notin V$, которое строится по схеме $A$ следующим образом: формулы выписываются в порядке следования их в схеме, причём буква $\gamma$ служит разделителем между формулами, каждая стрелка заменяется буквой $\alpha$, а каждая точка — буквой $\beta$.

*Например*, алгорифм $L_c$ левого присоединения слова $u \in V^*$, заданный схемой $$\{ \to \cdot u \},$$
будет иметь изображение: $\alpha \beta u$.

алгорифм $R_c$ правого присоединения непустого слова $u \in V^*$, заданный схемой в алфавите $V \cup \{\#\}$, где $\# \notin V$, имеющий вид (в сокращённой записи)

$$
\left\{
\begin{array}{l}
\# \eta \to \eta \#, \quad \eta \in V \\
\# \to \cdot u \\
\to \# \\
\to \bullet
\end{array}
\right.
$$

будет иметь такое изображение: $\# a_1 \alpha a_1 \# \gamma \dots \gamma a_n \alpha a_n \# \gamma \# \alpha \beta u \gamma \alpha \# \gamma \alpha \beta$,

где предполагается, что $V = \{a_1, \dots, a_n\}$.

В общем случае алгорифм, задаваемый схемой

$$
\left\{
\begin{array}{l}
u_1 \to [\bullet]{v_1} \\
\quad \dots \\
u_n \to [\bullet]{v_n}
\end{array}
\right.
$$

в алфавите $V$, будет иметь изображение в виде слова $u_1 \alpha [\beta]{v_1} \gamma \dots \gamma u_n \alpha [\beta]{v_n}$.

---

Вместо изображения нормального алгорифма часто используют запись нормального алгорифма — слово, определяемое как *перевод изображения в алфавит $V_0 = \{0,1\}$*. 

При построении записи НА в алфавите $V = \{a_1, \dots, a_n\}$ принято считать, что буквы $\alpha, \beta, \gamma$, фигурирующие в изображении, получают номера $n+1, n+2, n+3$ соответственно.

Так, запись алгорифма левого присоединения слова $abca$ в алфавите $V = \{a, b, c\}$ будет иметь вид:

$$
\begin{array}{c}
\underbrace{01111}_{\alpha} \underbrace{00111}_{\beta} \underbrace{11001}_{a} \underbrace{00110}_{b} \underbrace{01110}_{c} \underbrace{010}_{a}
\end{array}
$$

---

*Теорема:*  Для любого алфавита $V$ и любого НА $A$ в этом алфавите может быть построен НА $U$ над алфавитом $V \cup V_0 \cup \{\#\}$ такой, что для любого слова $x \in V^*$ имеет место условное равенство

$$
U(\text{ЗАЗ}\# x) \simeq A(x).
$$

Запись нормального алгорифма A обозначается как *ЗAЗ*.

алгорифм $U$, построенный согласно данной теореме, называют *универсальным нормальным алгорифмом*.

## **2.5. Теоремы объединения, разветвления и повторения НА (формулировки). Построение НА, распознающего равенство слов** [⬆⬆⬆](#top)


*Теорема объединения*: Каковы бы ни были нормальные алгорифмы $A$ и $B$ в алфавите $V$, может быть построен нормальный алгорифм $C$ над алфавитом $V$ такой, что для любого слова $x \in V^*$ выполняется условное равенство  
$$
C(x) \simeq A(x) B(x).
$$

---

*Теорема о разветвлении нормальных алгорифмов*:  Каковы бы ни были нормальные алгорифмы $A$, $B$ и $C$ в алфавите $V$, может быть построен алгорифм $D$ над алфавитом $V$ такой, что для любого слова $x \in V^*$ выполняется следующее:  

1) Если $\neg!C(x)$, то $\neg!D(x)$;  
2) Если $!C(x)$, то $ D(x) = A(x) \quad$ при $C(x) = \lambda$ и $D(x) = B(x) \quad$ при $C(x) \neq \lambda$.  

---

*Теорема о повторении НА*: Каковы бы ни были нормальные алгорифмы $A$ и $B$ в алфавите $V$, может быть построен нормальный алгорифм $C$ над алфавитом $V$ такой, что для любого слова $x \in V^*$ выполняется равенство  
$$  
y = C(x)  
$$  
тогда и только тогда, когда существует последовательность слов  
$$  
x = x_0, x_1, \dots, x_m = y  
$$  
такая, что  

- Если $m = 0$, то $y = x$ и $B(x) \neq \lambda$;  
- Если $m > 0$, то  
  $$  
  (\forall i = \overline{1,m}) \big((x_i = A(x_{i-1})) \land  
  (\forall j = \overline{0,m-1})(B(x_j) = \lambda)\big) \land (B(x_m) \neq \lambda).
  $$  

---

*Алгорифм распознавания равенства слов:* Построим алгорифм $EQ$ над алфавитом $V \cup \{*\}$ такой, что для любых двух слов $x, y \in V^*$ выполняется  
$$  
EQ(x * y) = \lambda \Leftrightarrow x = y.
$$  

Требуемый нормальный алгорифм можно построить, используя теоремы объединения и композиции, а именно:  
$$  
EQ(x * y) \simeq \text{Comp}(\text{Id}(x) * \text{Rv}(y)),
$$  
где $\text{Comp}$ — алгорифм, задаваемый схемой: 
 

$$
\left\{
\begin{array}{l}
\eta * \eta \to *, \quad \eta \in V \\
\ *\to \bullet
\end{array}
\right.
$$
 

Очевидно, что для любых двух слов $x, y \in V^*$ выполняется  
$$  
!\operatorname{Comp}(x * y) \land \operatorname{Comp}(x * y) = \lambda \Leftrightarrow x = y.
$$  


## **2.6. Определения разрешимого и перечислимого языка. Связь разрешимости и перечислимости. Примеры. Доказать невозможность разрешающего НА для языка, для которого невозможен полуразрешающий НА** [⬆⬆⬆](#top)

Язык (множество слов) $L \subseteq V^*$ называют *алгорифмически разрешимым*, если может быть построен нормальный алгорифм $A_L$ над алфавитом $V$ такой, что для каждого $x \in V^*$ имеет место $!A_L(x)$ и $A_L(x) = \lambda \Leftrightarrow x \in L$.  

Алгорифм $A_L$ называют *разрешающим алгорифмом* для языка $L$. 
 
Множество слов, содержащих хотя бы одно
вхождение заданного непустого слова, будет *алгорифмически разрешимо*.

Язык (множество слов) $L \subseteq V^*$ называют *алгорифмически перечислимым*, если существует нормальный алгорифм $N_L$ над алфавитом $V \cup V_0$ такой, что для каждого натурального числа $n$ имеет место $!N_L(n)$, $N_L(n) \in L$ и для каждого $x \in L$ осуществимо такое натуральное число $n$, что $N_L(n) = x$.  

Пример *перечислимого языка*: язык целых чисел. 

Любое алгорифмически разрешимое множество алгорифмически перечислимо. Обратное не верно.

*Теорема*: Если для языка $L \subseteq V^*$ невозможен полуразрешающий НА, то невозможен и разрешающий.

$\square$ Предположим противное, то есть предположим, что для языка $L \subseteq V^*$ не может быть построен полуразрешающий НА, но возможно построение разрешающего. Пусть $A_L$ — такой НА. Тогда по теореме разветвления, построим НА  
$$
B_L =_{A_L} (A_L \lor \textit{Null}).
$$  
Ясно, что для любого слова $x \in V^*$ имеет место $!B_L(x) \Leftrightarrow x \in L$, и НА $B_L$ является полуразрешающим вопреки предположению. $\blacksquare$

## **2.7. Проблемы применимости и самоприменимости для НА. Доказательство неразрешимости проблемы самоприменимости** [⬆⬆⬆](#top)

*Проблема применимости* для нормальных алгорифмов формулируется как частная и как общая.
 
*Частная проблема:* Дан нормальный алгорифм $A$ в алфавите $V$. Можно ли построить НА $B$ над алфавитом $V$ так, чтобы он был применим к любому слову $x \in V^*$, причем $B(x) = \lambda$ тогда и только тогда, когда $\neg!A(x)$.

Другими словами, НА $B$ должен быть разрешающим для дополнения области применимости НА $A$ относительно алфавита $V$.

*Общая проблема применимости*: Можно ли построить нормальный алгорифм $B$ над алфавитом $V \cup \{0,1\}$ такой, что для любых нормального алгорифма $A$ в алфавите $V$ и слова $x \in V^*$ выполнялось  
$$
!B(\text{ЗАЗ}\#x) = \lambda \Leftrightarrow \neg!A(x),
$$
где буква $\#$ не принадлежит алфавиту $V \cup \{0,1\}$.

*Проблема самоприменимости*: Можно ли построить такой алгорифм $B$ над алфавитом $V_0$, что для любого алгорифма $A$ в алфавите  
$$
V_1 = V \cup V_0 \cup \{\alpha, \beta\}, \quad \alpha, \beta \notin V \cup V_0
$$  
имеет место  
$$
!B(\text{ЗАЗ}) \text{ и } B(\text{ЗАЗ}) = \lambda \Leftrightarrow \neg!A(\text{ЗАЗ}).
$$  

*Лемма:* Невозможен нормальный алгорифм $B$ в алфавите $V \cup V_0$, применимый к записи произвольного нормального алгорифма $A$ в этом алфавите тогда и только тогда, когда алгорифм $A$ несамоприменим.

*Теорема:* Невозможен алгорифм над алфавитом $V_0$, применимый к записям тех и только тех алгорифмов в алфавите $V_1$, которые несамоприменимы.

$\square$ Пусть построен алгорифм $B$ над алфавитом $V_0$ так, что для всякого алгорифма $A$ в алфавите $V_1$ имеет место  
$$
!B(\text{ЗАЗ}) \Leftrightarrow \neg!A(\text{ЗАЗ}).
$$  
По теореме о переводе тогда может быть построен алгорифм $B_1$ в алфавите $V_0 \cup \{\alpha, \beta\}$ так, что для любого слова $x \in V_0^*$ имеет место условное равенство  
$$
B_1(x) \simeq B(x).
$$  
Пусть теперь алгорифм $B_2$ определен как естественное распространение алгорифма $B_1$ на алфавит $V_1$. Тогда оказывается, что этот алгорифм в алфавите $V_1$ таков, что для любого алгорифма $A$ в алфавите $V_1$ имеет место  
$$
!B_2(\text{ЗАЗ}) \Leftrightarrow \neg!A(\text{ЗАЗ}),
$$  
что невозможно в силу леммы. $\blacksquare$

## **2.8. Доказать алгорифмическую неразрешимость проблемы применимости для НА** [⬆⬆⬆](#top)

*Теорема:* Можно построить нормальный алгорифм $A$ в алфавите $V_2 = \{0,1,\alpha,\beta\}$ так, что невозможен нормальный алгорифм $B$ над алфавитом $V_0$, удовлетворяющий условию для любого слова $x \in V_0^*$:  
$$
!B(x) \Leftrightarrow \neg!A(x).
$$

$\square$ Пусть $\# \notin V_2$. По теореме об универсальном алгорифме построим алгорифм $U$ над алфавитом $V_2$ так, что для любого алгорифма $D$ в алфавите $V_2$ и любого слова $y \in V_2^*$ имеет место  
$$
U(\textit{ЗDЗ}\#y) \simeq D(y).
$$  

Теперь построим алгорифм $U_1$ над алфавитом $V_2$ так, что для любого слова $y \in V_2^*$  
$$
U_1(y) \approxeq U(y\# y).
$$  

Мы имеем НА $U_1$ над алфавитом $V_2$, но последний есть расширение алфавита $V_0$, и, следовательно, по теореме о переводе, может быть заменен вполне эквивалентным ему относительно алфавита $V_0$ алгорифмом $U_2$ в двухбуквенном расширении алфавита $V_0$, то есть в алфавите $V_2$. Для любого слова $x \in V_0^*$ будем иметь:  
$$
!U_1(x) \Leftrightarrow !U_2(x).
$$  

Утверждается, что это и есть искомый и указанный в условии теоремы алгорифм $A$ с неразрешимой частной проблемой применимости, то есть $A = U_2$.

Действительно, пусть построен алгорифм $B$ над алфавитом $V_0$ так, что  
$$
(\forall x \in V_0^*) \ (!B(x) \Leftrightarrow \neg!U_2(x)).
$$  
Тогда для любого алгорифма $D$ в алфавите $V_2$ будем иметь  
$$
!B(\textit{ЗDЗ}) \Leftrightarrow \neg!U_2(\textit{ЗDЗ}) \Leftrightarrow \neg!U_1(\textit{ЗDЗ}) \Leftrightarrow \neg!U(\textit{ЗDЗ}\#\textit{ЗDЗ}) \Leftrightarrow \neg!D(\textit{ЗDЗ}),
$$  
то есть НА $B$ оказывается полуразрешающим для проблемы самоприменимости для нормальных алгорифмов в алфавите $V_2$, что невозможно.

Итак, проблема применимости для нормальных алгорифмов является неразрешимой. $\blacksquare$

## **2.9. Понятие порождающей грамматики и вывода в порождающей грамматике. Определение языка, порождаемого грамматикой. Примеры** [⬆⬆⬆](#top)


*Порождающая грамматика* задается упорядоченной четверкой:  
$$
G = (V, N, S, P)
$$  
где:  

- $V$ — терминальный алфавит (буквы — терминалы).
- $N$ — нетерминальный алфавит (буквы – нетерминалы), причем пересечение этих алфавитов пусто.
- $S \in N$ — выделенный нетерминальный символ, называемый аксиомой.  
- $P$ — конечное множество правил вывода (или продукций), где каждое правило имеет вид:  
  $$
  \alpha \to \beta,
  $$
  где $\alpha, \beta \in (V \cup N)^*$ — слова в объединенном алфавите $V \cup N$, причем левая часть правила, слово (цепочка) $\alpha$, содержит хотя бы одно вхождение нетерминала.  

Стрелка есть метасимвол, не принадлежащий ни одному из упомянутых алфавитов, отделяющий левую часть правила (цепочку $\alpha$) от правой ($\beta$).  

---

Пусть $\gamma, \delta \in (V \cup N)^*$. Говорят, что цепочка $\delta$ _непосредственно выводима_ в грамматике $G$ из цепочки $\gamma$, если существуют цепочки $\gamma_1, \gamma_2$ и правило вывода $\alpha \to \beta \in P$ такие, что:
$$
\gamma = \gamma_1 \alpha \gamma_2, \quad \delta = \gamma_1 \beta \gamma_2,
$$
то есть цепочка $\delta$ получена из цепочки $\gamma$ путем замены некоторого вхождения левой части правила (не обязательно первого, в отличие от нормального алгоритма) на правую часть.  

Указанное правило может быть выбрано произвольно.

---

*Язык $L(G)$, порождаемый грамматикой $G$*, есть, по определению, множество всех слов в терминальном алфавите, выводимых из аксиомы:

$$
L(G) \rightleftharpoons \{x : S \vdash^*_G x, x \in V^*\}
$$


## **2.10. Классификация грамматик и языков. Связь с понятиями перечислимого и разрешимого языка** [⬆⬆⬆](#top)

Грамматики классифицируются по ограничениям на правила вывода.

1) *Грамматики типа 0*  
Нет никаких дополнительных ограничений (кроме исходного: левая часть каждого правила должна содержать нетерминал).  

2) *Неукорачивающие (НК-) грамматики*  

$$
\alpha \to \beta, \quad |\alpha| \le  |\beta|
$$

Грамматика примера (5) является неукорачивающей, тогда как грамматика первого примера таковой не является из-за наличия правил с пустой правой частью.  

3) *Контекстно-зависимые (КЗ-) грамматики*  

$$
\alpha A \beta \to \alpha \xi \beta,$$ $$\quad A \in N,$$ $$\alpha, \beta, \xi \in (V \cup N)^*, \xi \neq \lambda.
$$

Видно, что КЗ-грамматика является неукорачивающей.  

4) *ОКЗ-грамматики*  
Допуская в КЗ-правиле пустую цепочку $\xi$, получим обобщенную КЗ-грамматику (ОКЗ).  

5) *Контекстно-свободные (КС-) грамматики* 

$$
A \to \xi, \quad A \in N,$$ $$\alpha, \beta, \xi \in (V \cup N)^*
$$

6) *Линейные грамматики*  

$$
A \to uBv \mid u; \quad A, B \in N, u \in V^*
$$

7) *Праволинейные грамматики*  

$$
A \to uB \mid u; \quad A, B \in N, u \in V^*
$$

8) *Леволинейные грамматики*

$$
A \to Bu \mid u; \quad A, B \in N, u \in V^*
$$

9) *Регулярные (автоматные) грамматики*  

$$
A \to aB \mid a; \quad A, B \in N, a \in V \cup \{\lambda\}
$$

---

*Теорема:*
1. Язык перечислим тогда и только тогда, когда он порождается некоторой
грамматикой типа 0.
2. Всякий КЗ-язык разрешим, но обратное неверно.

## **2.11. Понятие МП-автомата и языка, допускаемого МП-автоматом. Связь между МП-автоматами и КС-грамматиками. Примеры** [⬆⬆⬆](#top)

Формально *МП-автомат* определяется кортежем:  

$$
M = (Q, V, \Gamma, q_0, F, Z_0, \delta),
$$

где  

$Q$ - конечное множество состояний,  
$V$ - входной алфавит (алфавит ленты),  
$\Gamma$ - магазинный алфавит,  
$q_0 \in Q$ - начальное состояние,  
$F \subseteq Q$ - подмножество заключительных состояний,  
$Z_0 \in \Gamma$ - начальный магазинный символ,  
$\delta$ - функция переходов, являющаяся отображением  

$$
\delta: Q \times (V \cup \{\lambda\}) \times \Gamma \to 2^{Q \times \Gamma^*}.
$$

Функция переходов задается в виде системы команд, конечного множества команд вида:  

$$
qaZ \to r\gamma,
$$

где  

$q, r \in Q$ - состояния,  
$a \in V \cup \{\lambda\}$, $Z \in \Gamma$, $\gamma \in \Gamma^*$.  

---

*Язык, допускаемый МП-автоматом* $$M = (Q, V, \Gamma, q_0, F, Z_0, \delta),$$ определяется как:  

$$
L(M) = \{ x : (q_0, x, Z_0) \vdash^*_M (q_f, \lambda, \lambda); x \in V^*, q_f \in F \}.
$$

*Теорема*: Язык является контекстно-свободным тогда и только тогда, когда он
допускается некоторым МП-автоматом.

--- 

*Например*, для КС-грамматики палиндромов в алфавите $\{a, b\}$  

$$
S \to aSa \mid bSb \mid aa \mid bb \mid a \mid b \mid \lambda
$$

получим такой эквивалентный МП-автомат:  

$$
qaS \to qSa \mid qa \mid q\lambda
$$
$$
qbS \to qSb \mid qb \mid q\lambda
$$
$$
q\lambda S \to q\lambda
$$
$$
qaa \to q\lambda
$$
$$
qbb \to q\lambda
$$

И если в исходной форме системы команд (без учета первой буквы) МП-автомат должен «выбирать» из семи команд, то в данной форме выбор ограничен тремя командами.

